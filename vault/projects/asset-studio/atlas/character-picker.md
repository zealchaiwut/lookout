---
feature: Character Picker
files_read:
  []
traced: 2026-08-13
stale: false
---


## What

Character Picker — traced from `tests/test_character_picker__90.py` through 0 source file(s).

## Entry Points

- `tests/test_character_picker__90.py` (tracing origin)

## Related Issues

- #111 — [follow-up] Unused account param in build_character_prompt
- #106 — [follow-up] Hoist repeated inner 'import json' to module level in characters_cutouts

## Flowchart

```mermaid
flowchart LR
```

## Key Files

_No source files could be read during tracing._


## Open Questions

<!-- OPEN QUESTION: `pytest` imported in `test_character_picker__90.py` but `pytest.py` not found in source — handler unresolved -->
<!-- OPEN QUESTION: `httpx` imported in `test_character_picker__90.py` but `httpx.py` not found in source — handler unresolved -->
<!-- OPEN QUESTION: Entry point `tests/test_character_picker__90.py` imports no local modules — no implementation files could be traced -->
