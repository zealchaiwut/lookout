---
feature: Training log
files_read:
  []
traced: 2026-08-13
stale: false
---


## What

Training log — traced from `tests/test_training_log.py` through 0 source file(s).

## Entry Points

- `tests/test_training_log.py` (tracing origin)

## Related Issues

- #1672 — [follow-up] Log instead of silently swallowing malformed sets_json in get_strength_tss_per_set_for_workout
- #848 — [follow-up] weekly_count stepper not pre-populated from today's existing log value
- #642 — Change Training Log Close Button to Grey

## Flowchart

```mermaid
flowchart LR
```

## Key Files

_No source files could be read during tracing._


## Open Questions

<!-- OPEN QUESTION: Trace from `tests/test_training_log.py` reached 79 files, over the 25-file limit — this entry point describes the application, not `Training log`. No diagram emitted; add a test or a source file named for this feature -->
